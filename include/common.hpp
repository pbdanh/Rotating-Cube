#pragma once

#include <SDL.h>

const int SCREEN_WIDTH = 600;
const int SCREEN_HEIGHT = 400;

const int X0 = SCREEN_WIDTH / 2;
const int Y0 = SCREEN_HEIGHT / 2;

const float viewerCoordinate = 1300;
const float screenCoordinate = 500;